# race-analyzer-app — システム仕様引き継ぎ書（AI向け）

> このドキュメントは、本システムの目的・構成・データ・評価ロジックを
> 別のAI（GPT等）に引き継ぐためのものです。これ単体で全体像が掴めるよう書いています。

---

## 1. このシステムは何か（目的）

藤田（個人）専用の**競馬データ閲覧・分析Webアプリ**。
JRA競馬の有料データ提供サービス **JRDB** から取得した生データを取り込み、
出走馬を独自ロジックで事前評価し、レースごとに「どの馬がどういう状態か」を
俯瞰できるようにすることが目的。

重要な思想（アプリ内「AIの位置づけ」タブにも明記）:
- **AIは予想者ではない。** 馬券の買い目を断定しない。
- 役割は「人間が見落とす状態・条件・適性を可視化し、判断材料を整える」こと。
- 4原則: ①事実ベース ②条件適性の明示 ③状態（調子）の可視化 ④過信させない。

ビジネス的背景: マルチユーザー想定なし。個人運用のため認証は最小限、
サーバサイドのDB APIやSSRは持たない「ローカル生成 → 静的配信」設計。

---

## 2. システム全体構成（2リポジトリ）

データ生成（重い処理）と表示を分離している。

```
┌─────────────────────────┐      ┌──────────────────────────────┐
│ backend（Python）        │      │ race-analyzer-app（React SPA）│
│ ~/projects/backend      │      │ ~/projects/race-analyzer-app │
│                         │      │                              │
│ JRDB生データ取込         │      │ ブラウザで race.db を直接読む  │
│ → SQLite(race.db) 生成   │ ───▶ │ （sql.js / WASM）            │
│ → 評価を事前計算          │      │ → 4タブUIで表示               │
└─────────────────────────┘      └──────────────────────────────┘
        ↑ローカルPCで実行                    ↑Vercelで静的ホスティング
                       Supabase Storage 経由でDBファイルを受け渡し
```

### データフロー（非自明・最重要）
1. `~/projects/backend` の Python パイプラインが JRDB の生テキストファイルを
   パースして SQLite `race.db` を生成・更新（`import_jrdb.py`）。
2. 同パイプラインが全レース×全馬の**評価を事前計算**し、`race.db` 内の
   `race_evaluations` テーブルに JSON で保存（`evaluate_all.py`）。
3. `scripts/push_db.sh` が `race.db`（gzip圧縮）を Supabase Storage
   バケット `race-db` に PUT。
4. ブラウザ: Supabase Auth でログイン → Storage から `race.db` を署名URLでDL
   → Cache API で永続化 → **sql.js（SQLite の WASM版）でブラウザ内で直接クエリ** → UI描画。

→ つまりフロントは**完全な表示専用**。評価ロジック・データ生成はすべて backend 側にある。
   フロントはサーバAPIを一切叩かず、SQLファイルをローカルで読むだけ。

---

## 3. 技術スタック

| 層 | 採用技術 |
|---|---|
| フロント | React 19 + Vite + Tailwind CSS v4（SPA） |
| アイコン | lucide-react |
| クライアントDB | sql.js 1.13（SQLite WASM）+ Cache API で永続化 |
| 認証 | Supabase Auth（メール/パスワード、Auto Confirm） |
| データ配信 | Supabase Storage バケット `race-db`（gzip済み race.db） |
| ホスティング | Vercel（GitHubプライベートリポジトリから自動デプロイ） |
| バックエンド | Python + SQLAlchemy + SQLite（ローカル実行のみ。常駐サーバなし） |
| 本番URL | https://race-analyzer-app.vercel.app/ |

※ Supabaseの公開設定（URL/anon key）はコードに埋め込み済みで、Vercel側の環境変数設定は不要。

---

## 4. 取り込んでいるデータ（JRDB各ファイル → SQLiteテーブル）

JRDBは複数の固定長テキストファイルを提供する。それぞれを別テーブルへ取り込む。

| JRDBファイル | 内容 | 取込先テーブル | 主な用途 |
|---|---|---|---|
| **BAC** | 番組（レース）データ | `jrdb_races` | レース日程・距離・馬場・グレード・発走時刻 |
| **UKC** | 馬基本データ（馬マスタ） | `jrdb_horses` | 馬名・血統・性齢など |
| **SED / ZED** | 成績（過去走）データ | `jrdb_results` | 着順・タイム・上り3F・人気・馬体重・騎手・斤量・オッズ。SEDは当週、ZEDは累計 |
| **KYI** | 出走馬データ（出走前情報） | `jrdb_entries` | **IDM**（JRDB総合指数）、各種印、厩舎指数など。能力スコアの中核 |
| **CYB** | 調教分析データ | `jrdb_training` | 追切指数・仕上指数など。**本気度判定**の基礎 |
| **CHA** | 調教本追切データ | `jrdb_hard_training` | 本追切時計・終いF指数など。本気度の補助 |
| **KAB** | 開催（馬場状態）データ | `jrdb_conditions` | 馬場状態・トラックバイアス |
| （拡張） | 成績拡張 | `jrdb_result_ext`, `jrdb_horse_stats` | 適性・脚質などの付帯統計 |

### 主要テーブルのカラム例
- `jrdb_results`（過去走 / SED由来、1レース1馬で1行）:
  着順 `rank`、頭数 `field_size`、走破タイム `finish_time`、上り3F `last_3f`、
  人気 `popularity`、確定単勝オッズ `win_odds`、斤量 `weight_carried`（×10）、
  馬体重 `horse_weight`、馬体重増減 `horse_weight_delta`（前走比・符号付き）、
  騎手 `jockey_name`、クラス情報 `kind_code`/`condition_code`/`grade`。
- `jrdb_entries`（KYI由来）: `idm`（IDM = JRDB独自の能力総合指数）、各種印、厩舎指数 `stable_idx` ほか。
- `race_evaluations`: `race_key`（PK）, `evaluated_at`, `evaluator_version`, `payload`（評価結果JSON一式）。

データ規模感: アプリ起動時にDB統計（レース総数 / 登録馬数 / 過去走レコード数 / 対象期間）を表示。

---

## 5. 評価ロジック（backend / `main.py` の `compute_race_evaluation`）

レースごとに全出走馬を評価し、JSONで `race_evaluations` に保存する。
評価器バージョンは `evaluator_version`（現状 v1.1）で管理し、ロジック改修時にバンプ→再計算。

### 5-1. 馬単位の主要スコア（`evaluate_horse`）

- **能力スコア（abilityScore, 0–100）**
  - 合成式: `IDM正規化 × 0.6 + 3着内率 × 0.4`
  - IDM正規化: 過去5走のIDM平均を「IDM20→0点・IDM70→100点」で線形正規化。
    過去走IDMが無ければ今回出走時IDM→それも無ければ3着内率のみにフォールバック。
  - スコアから能力ランク（S/A/B/...）も導出。

- **一致度スコア（matchScore, 0–100）= 今回条件への適性**
  - 過去走のうち「距離が±200m以内 かつ 同じ馬場（芝/ダート）」のレースを抽出し、
    その中での3着内率。該当走が無ければ50（中立）。

- **評価記号（◎ / ○ / △ / ×）**
  - 過去走3走未満は一律 △（データ不足）。
  - ◎: ability≥60 かつ match≥60
  - ○: ability≥40 かつ match≥40
  - △: ability≥20 または match≥30
  - ×: それ以外

- **勝つ条件 / 負ける条件**: 3着内だった走 / 6着以下だった走から、距離・馬場・コースの最頻値を抽出。
- **AI短評（comment）**: 評価記号に応じた定型文（過去走数・3着内回数・同条件相性を埋め込む）。
- **過去走テーブル**: 直近5走の日付・クラス・距離馬場・着順・頭数・人気・タイム・オッズ・上り3F・騎手・斤量・馬体重±。

### 5-2. 状態・文脈の判定（バッジ）

- **フェーズ判定（determine_phase）**: 直近成績の推移から馬の状態を分類
  → 上昇 / 安定 / 条件再設定 / ムラ / 下降 / 休養明け / 経験浅。
- **本気度（determine_motivation）**: CYB（調教分析）+ CHA（本追切）から加点方式でスコア化
  → 追切指数≥70=+3 / ≥60=+2 / 仕上≥60=+1 / 一週前変動≤-5=+1 / 終いF指数≥60=+1（末脚仕上がり）など。
- **昇級試金石（determine_upgrade）**: 前走勝利後にクラスを上げた初戦かを判定。
- **クラス挑戦（determine_class_challenge）** / **賞金追走（determine_prize_chase）**: 格上挑戦・目標レース志向の検出。
- **レース意味分類（classify_race_type）**: 出走馬のフェーズ構成からレース全体の性質を分類。

### 5-3. レース内の相対評価

- 各指標（能力 / 本気度 / 追切 / 終いF）について**レース内順位**を付与。
- **王冠バッジ（crown）**: 上記4指標のうち何個で「レース内1位」かを集約（4冠＝最強の象徴）。
- **レース内1位タグ**: 能力1位 / 本気1位 / 追切1位 / 終い1位 を個別表示。

---

## 6. フロントUI（4タブ構成）

1. **JRDB実データ**: DB統計、開催日セレクタ（直近60日）、開催場ごとのレース一覧
   （グレード/レース名/距離/頭数/発走時刻）。レースクリックで評価ロード→一覧タブへ。
2. **一覧 & 詳細**:
   - 出走馬テーブル（馬番/馬名/騎手/能力ランク/本気度/一致度/評価記号 + 各種バッジ）。
   - フィルタ（全/◎/○/△/×）、ソート（評価/馬番/能力/一致度/フェーズ/本気度）、馬名・騎手検索。
   - 詳細ペイン: AI短評、勝つ条件/負ける条件、過去走テーブル（SEDフル項目）。
     上り3Fは閾値カラーリング（≤33.5 / ≤34.5 / ≤36.0 / それ以上）。
   - 左右ペインはドラッグでリサイズ可（localStorage保存・ダブルクリックでリセット）。
3. **全体評価**: 評価別（◎/○/△/×）カウントと上位3頭、能力上位Top3、一致度上位Top3、
   リスク馬（能力高・一致度低）リスト。
4. **AIの位置づけ**: システム哲学（4原則）と拡張機能のComing Soon一覧。

---

## 7. 運用フロー（週次データ更新）

```bash
# 1. JRDBから取り込み（race.db更新）
cd ~/projects/backend && ./venv/bin/python import_jrdb.py
# 2. 評価を事前計算（race_evaluationsへ）
cd ~/projects/backend && ./venv/bin/python evaluate_all.py
# 3. Supabase StorageへPUT
cd ~/projects/race-analyzer-app && bash scripts/push_db.sh
# 4. ブラウザで「DB再取得」 or ハードリロードで反映
```

データが古い/反映されない時は、まず `push_db.sh` が走ったかを疑う。

---

## 8. 現状ステータスとロードマップ

- **現状**: リリース済みフェーズの実装は完了（認証・DB読込・4タブ・評価バッジ一式）。
- **Coming Soon（未実装）**: 当日オッズ連携 / 馬体重変化追跡 / 天候連動シミュレート /
  EV（期待値）計算 / 買い目（フォーメーション）提案 / 当日トラックバイアス補正。

---

## 9. AIに引き継ぐ際の注意点

- このアプリは**馬券の買い目を出すツールではない**。提案や改修も「判断材料の可視化」の枠を超えない。
- データの正本は JRDB（有料）。再配布不可。生データの扱いに注意。
- 「UI/表示/Vercel/ログイン/レース一覧」の話 = `~/projects/race-analyzer-app`（フロント）。
- 「JRDB取込/race.db更新/評価ロジック」の話 = `~/projects/backend`（Python）。
- 紛らわしい別物: `~/keiba/`（keiba.db 等）は無関係な旧競馬システム。混同しない。
- 評価ロジックを変えたら `evaluate_all.py --full` で全再計算し、`evaluator_version` をバンプする。
