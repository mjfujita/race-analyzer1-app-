# Race Analyzer App（フロントエンド） - プロジェクトガイド

> このファイルは Claude Code 用のプロジェクト指示書です。
> **最新の実装状況は必ず [STATUS.md](./STATUS.md) を参照すること**（このファイルは思想・規約・ドメイン定義などの安定情報を担う）。

## プロジェクト概要

**プロダクト名**: Race Condition Analyzer
**目的**: 前日までの情報で構造化指標を提供し、当日のオッズ・パドックは人間判断
**利用範囲**: 個人利用のみ（JRDB規約遵守）

## システム全体構成（2フォルダ構成）

このプロダクトは2つのフォルダに分かれている。役割を混同しないこと。

| フォルダ | 役割 | 技術 |
|---|---|---|
| `~/projects/race-analyzer-app`（このフォルダ） | フロントエンドSPA・表示 | React 19 + Vite + Tailwind v4 + Supabase + sql.js |
| `~/projects/backend` | JRDBデータ取込み・評価ロジック・`race.db`生成 | Python（SQLAlchemy / SQLite） |

データの流れ:
```
backend（Python）でJRDB取込み＆評価プレ計算 → race.db 生成
  → Supabase Storage バケット race-db に PUT
    → race-analyzer-app がブラウザで race.db を sql.js で直接読んで表示
```

**バックエンドサーバは常駐しない。** フロントは Supabase Storage から `race.db` を取得し、ブラウザ内（sql.js + Cache API）で完結する SPA。

## このフォルダ（フロント）の要点

- 認証: Supabase Auth（メール/パスワード）
- データ: Supabase Storage `race-db` の `race.db` を取得、Cache API で永続キャッシュ
- ホスティング: Vercel（GitHubプライベートリポジトリから自動デプロイ）
- 本番URL: https://race-analyzer-app.vercel.app/
- 主要ファイル: `src/App.jsx`（メインUI）, `src/lib/db.js`（sql.js読込）, `src/supabase.js`
- 機能の詳細（4タブ構成・バッジ・フィルタ等）は [STATUS.md](./STATUS.md) を見ること

```bash
# 開発起動
cd ~/projects/race-analyzer-app && npm run dev
```

## ドメイン定義（フロント・バックエンド共通の言葉）

### 馬フェーズ判定（6+分類）
- 🔼 上昇 / ➡️ 安定 / 🔀 条件再設定 / 🔄 ムラ / 🔽 下降 / 🛌 休養明け / 🆕 経験浅

### レース意味分類
- 🛌 休養明け多発 / 🔼 上昇馬選別 / ➡️ 滞留馬決着 / 🔀 条件替わり検証 / 🌪 荒れ吸収

### 本気度判定
- 仕上がり万全 / 標準的に良好 / 普通 / 物足りず

### 評価記号
- ◎ / ○ / △ / ×（能力スコア・条件一致度から算出。算出ロジックは backend `main.py::compute_race_evaluation`）

> 判定ロジックの実体は `~/projects/backend` 側にある。フロントは backend が `race.db` に書いた結果を表示するだけ。

## ユーザー情報

- 藤田将（株式会社MJ代表）
- WEBシステム開発（保育・教育ICT）が本業 / 競馬は個人の楽しみ
- 思考特性: 本質志向、時間効率重視、構造重視

## システムの哲学（AIの位置づけ）

- AIは予想者ではない。前日までの構造化指標を提供する役。
- 当日朝のオッズ・直前情報・パドックは人間が統合判断する。

## JRDB規約の制約

- 個人利用OK（藤田個人のPCで本人のID/パスワード）
- 商用利用NG（MJ社事業に組み込み不可）/ アカウント共有NG / 再配布NG

## 開発スタイル

- 個人利用なので制限最小限 / 完璧設計より試行錯誤 / 時間優先 / 作って修正

## 関連

- バックエンド: `~/projects/backend`（`STATUS.md` あり）


---

# 全プロダクト共通・開発運用方針（恒久ルール）

> 設定日: 2026-07-28 / 正本: `~/.claude/CLAUDE.md`（全プロダクト共通）。内容は同一。矛盾した場合は正本を優先。
> 本セクションは全リポジトリ共通。**このリポジトリ固有の設計思想・制約・手順（本ファイルの他章）が優先**する。

## 役割分担

依頼者が示すのは「実現したいこと / 現在の課題 / 利用者にとって必要な状態 / 事業上の目的 / 完成イメージ / 重要な制約」。
Claude Code は、それを基に**技術調査・設計・実装・検証までを自律的に進める**。
依頼者を、Claude Code と他の支援者との間の**伝言役・コード確認役・細かな進行管理役にしない**。

## 通常依頼で一連に実施する工程

ブランチ・未コミット差分・リポジトリ状態の確認 → 関連する既存コード / DB / 画面 / API / 型 / テストの調査 → 既存設計・過去の実装パターンの確認 → 影響範囲の特定 → 実装方針の決定 → 作業ブランチ作成 → コード / UI / DB / API / 型 / テストの変更 → lint・型チェック・ビルド・関連テスト → 既存機能への回帰影響の確認 → ローカル / Preview / ステージングでの確認 → 差分・結果・注意点・未対応事項の整理 → **本番反映直前の状態で停止して報告**。

調査結果や実装方針の提示だけで停止しない。実装可能な依頼は、原則として**検証済みの状態まで**進める。

## 途中確認を求めない事項（自分で判断する）

軽微な文言修正 / ボタン・表示要素の配置 / 既存パターンに沿った UI 調整 / 型エラーの修正 / lint エラーの修正 / import・命名の整理 / テストコードの追加 / 既存コンポーネントの再利用 / 小規模なリファクタリング / ローディング・空状態・エラー表示 / 入力チェック / 既存仕様に沿った権限チェック / migration 名・ファイル名・関数名等の技術的命名 / 既存機能を壊さないために必要な軽微な補完 / 誤操作を防ぐための確認表示 / 操作履歴・訂正・復旧を可能にする安全措置。

## 作業を止めて確認してよい条件（これ以外では止めない）

1. **不可逆な変更** — 本番データの削除 / 大量データの更新 / 復元困難な migration / 既存データ形式の破壊的変更 / データ移行による情報欠損の可能性
2. **重大な仕様矛盾** — 既存の正式仕様と明確に矛盾 / 選択により利用者の業務フローが大きく変わる / 既存顧客への影響が大きい / 後方互換性を維持できない
3. **事業判断が必要** — 課金・料金 / 契約条件 / 売上計上 / 商流 / 顧客責任範囲 / 法務 / 規約 / 個人情報の利用目的・外部提供・保持期間 / 管理者権限の大幅な変更
4. **情報が不足して実行できない** — 外部サービスの認証情報 / 契約状況の確認 / API 仕様が取得できない / 対象環境・対象顧客を特定できない
5. **本番操作が必要** — 本番デプロイ / main へのマージ / 本番 DB への migration 適用 / 本番環境変数の変更 / 本番外部サービスの設定変更 / 本番データの操作

## 判断に迷う場合の優先順位

1. 利用者と子どもの安全 → 2. 個人情報と機密情報の保護 → 3. 既存機能を壊さないこと → 4. 変更を元に戻せること → 5. 誤操作や勘違いが起きにくいこと → 6. 操作履歴や監査証跡を残すこと → 7. 既存設計との整合性 → 8. 利用者が説明を読まなくても迷いにくいこと → 9. 保守性 → 10. 実装速度

**人間もAIも間違える前提**で、可逆性・履歴・訂正・復旧・安全な初期値・段階実行を重視する。

## 実装時の標準ルール

- main へ直接コミットしない
- 既存の未コミット変更を勝手に破棄しない
- 依頼範囲外の大規模変更を行わない / 関係のないファイルを変更しない
- 本番用秘密情報をコードへ記載しない。実データ・個人情報・認証情報をコミットしない
- migration は原則として追加方式とし、既存 migration を書き換えない
- 破壊的変更には退避・移行・復旧方法を用意する
- 既存の権限・監査・RLS・認証方式を迂回しない
- エラー時に処理途中の不整合が残らないようにする
- 既存機能への回帰テストを行う
- UI 変更時は、通常状態だけでなく**空状態・エラー状態・権限なし・読み込み中**も確認する

## 確認環境

確認はローカル / Preview / 開発 / ステージングで行う。**本番に反映しなければ確認できない設計を避ける。**
確認用 URL を発行できる場合は報告に含める。DB 変更は原則ステージングまでに留め、**本番 DB には適用しない**。

## 本番に関する禁止事項（明示的な承認がない限り禁止）

main へのマージ / main への直接 push / production デプロイ / 本番 Supabase への migration 適用 / 本番 DB のデータ変更 / 本番環境変数の変更 / production alias の切り替え / 本番外部サービスの設定変更 / 本番ストレージの変更 / 本番データの削除。

**「本番には上げないでください」という依頼がなくても、通常の実装依頼は本番未反映で停止する。**

## 完了の定義

実装完了 / lint 通過 / 型チェック通過 / ビルド通過 / 関連テスト通過 / 主要な回帰確認完了 / DB 変更内容の整理 / 確認方法の提示 / 既存機能への影響の整理 / 未対応事項の明示 / 本番反映手順の整理 / **本番環境には未反映**。

## 完了報告の形式

- **実装結果** — 実装した内容 / 利用者から見た変更点 / 対象画面・対象機能
- **技術変更** — 変更した主なファイル / DB 変更の有無 / migration の有無 / API 変更の有無 / 権限変更の有無
- **検証結果** — lint / 型チェック / ビルド / 自動テスト / 手動確認 / 回帰確認
- **確認方法** — 確認用 URL / 確認手順 / 必要なテストアカウントや権限
- **影響と注意点** — 既存機能への影響 / 既知の制約 / 未対応事項 / リスク
- **本番反映** — 現在のブランチ / コミットハッシュ / 本番反映に必要な作業 / migration 適用順 / ロールバック方法 / **本番未反映であることの明記**

## 依頼者へ確認する内容の絞り込み

技術的な経緯を長く説明せず、**判断が必要な事項だけ**を提示する。

- 悪い例: 「実装方法を A と B で迷っています」「このファイルを変更してよいですか」「この命名でよいですか」「この UI で進めてよいですか」「migration を作ってよいですか」
- 良い例: 「この変更を行うと既存顧客の過去データ表示が変わります。既存表示を維持するか、新方式へ統一するかの事業判断が必要です」「顔データを外部サービスへ送信する設計となるため、利用目的と保存期間の決定が必要です」「本番反映可能な状態です。main へのマージと本番 migration の適用承認が必要です」

---

## リポジトリ固有情報（2026-07-28 時点）

※ プロジェクト概要・2フォルダ構成・ドメイン定義・JRDB 規約の制約は本ファイル上部の各章が正本。

| 項目 | 内容 |
| --- | --- |
| プロダクト名 | Race Analyzer App（フロントエンド） |
| 主な利用者 | 藤田（個人利用） |
| 技術構成 | Vite + React / Supabase（Auth + Storage）/ Tailwind CSS / sql.js |
| 開発環境 | `npm run dev` |
| ステージング環境 | なし（本番のみ） |
| 本番環境 | Vercel プロジェクト `race-analyzer-app` / https://race-analyzer-app.vercel.app/ |
| テストコマンド | 未確認（テストスクリプトなし） |
| ビルドコマンド | `npm run build` |
| lint | 未確認（lint スクリプトなし） |
| migration 管理 | 該当なし（DB は backend が生成した `race.db` を Storage から取得） |
| Preview 作成方法 | Vercel（GitHub プライベートリポジトリから自動デプロイ）。ブランチ push で Preview |
| 本番反映方法 | main への push で Vercel 自動デプロイ（＝**本番反映**。明示承認なしに main へ push しない） |
| 本番反映時の注意 | `npm run push-db` / `scripts/push_db.sh` は Supabase Storage の本番 `race.db` を上書きする。明示承認なしに実行しない |
| 絶対に変更してはいけない設定 | `VITE_RACE_DB_BUCKET` / `VITE_RACE_DB_OBJECT`（Storage の参照先） |
| 正式な基準ブランチ | `main`。リモート: https://github.com/mjfujita/race-analyzer1-app-.git |
